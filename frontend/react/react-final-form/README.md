
# React-final-form

```js
<Form
    initialValues={initialValuesGeneralForm}
    onSubmit={onSubmitA}
    initialValuesEqual={isEqual}
    render={({ handleSubmit }) => (
        <form onSubmit={handleSubmit}>
            ...
        </form>
    )}
    />
```


